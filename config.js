export const CONFIG = {
    PAPER_TRADING: true,

    START_BALANCE: 1000,

    TRADE_SIZE_PCT: 0.2,      // % of capital per trade
    MIN_SPREAD: 0.001,        // 0.1%

    LOOP_DELAY: 75,           // faster loop
};